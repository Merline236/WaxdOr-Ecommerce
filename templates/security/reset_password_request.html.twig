{% extends 'base.html.twig' %}

{% block title %}Réinitialistaion de mot de passe{% endblock %}

{% block body %}
<section class="container my-3">
    <div class="row">
        <div class="col-4">
            <h3>Réinitialistaion de mot de passe</h3>
            {{ form_start(requestPassForm) }}
            {{ form_row(requestPassForm.email) }}
                <button type="submit" class="btn btn-lg btn-warning mt-3">Envoyer</button>
            {{ form_end(requestPassForm) }}
        </div>

    </div>
</section>
    

{% endblock %}
